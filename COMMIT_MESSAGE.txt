feat: improve code quality, developer experience, and project structure

Major improvements to make the project more professional and maintainable:

**Code Quality & Tooling:**
- Add Prettier configuration for consistent code formatting
- Fix all TypeScript and ESLint errors (2595 errors → 0)
- Add environment variable validation with clear error messages
- Update ESLint config to ignore venv and node_modules

**Developer Experience:**
- Create professional startup scripts with colored terminal output
- Simplify npm scripts: combine checks into `npm run check` and fixes into `npm run fix`
- Add health check endpoints (/health, /health/ready, /health/live) for monitoring
- Improve error handling with proper TypeScript types

**Project Structure:**
- Organize documentation into dedicated `docs/` folder
- Remove internal development docs (industry readiness, improvements summary)
- Add LICENSE file (MIT)
- Add .env.example template for easy setup
- Remove unused AWS/Docker files

**Scripts:**
- Add cross-platform scripts (Mac/Linux .sh and Windows .bat)
- Professional terminal output with borders and color coding
- Auto-install missing dependencies
- Single kiwi emoji branding

**Type Safety:**
- Replace all `any` types with proper TypeScript types
- Fix React hooks violations
- Improve error handling types
- Fix impure function calls in render

This commit brings the project to production-ready code quality standards.

